Generated experiment artifacts (figures, metrics CSVs, model checkpoints) live here
and **are committed** so the team can view outcomes without re-running the experiments.
Only `data/` (the auto-downloaded dataset) and the virtualenv are gitignored.

