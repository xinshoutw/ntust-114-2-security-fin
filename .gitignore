# Datasets (auto-downloaded at runtime; reproducible, so not shared)
/data/orl_faces/
/data/**/*.png
/data/**/*.pgm
/data/**/*.mat
/data/**/*.tar.gz
/data/**/*.zip

# Python / uv (never share the environment)
__pycache__/
*.py[cod]
.venv/
.uv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# OS
.DS_Store

# Note: results/ (figures, metrics CSVs, model checkpoints) ARE committed so
# team members can view outcomes without re-running the experiments.
